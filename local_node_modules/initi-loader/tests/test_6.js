export default function run() {
    const MESSAGES = {
        success: "Test 6: btoa and atob from JSDOM worked correctly",
        error: "Test 6: btoa and atob from JSDOM didn't work correctly",
    }
    return new Promise((resolve, reject) => {
        console.debug("::Starting test_6.js::");

        try {
            const btoaResult = btoa("Hello World");
            const atobResult = atob(btoaResult);
            console.log(MESSAGES.success);
            resolve();
        } catch (error) {
            console.error(MESSAGES.error, error);
            throw new Error(MESSAGES.error);
        }
    })
}

import fs from "fs";
import compileCodeToModule from "./compileCodeToModule.js";

export default function getLoader(loaderPath) {
    const loaderScript = fs.readFileSync(loaderPath, "utf8");
    const Loader = compileCodeToModule(loaderScript, { filename: loaderPath, exportName: "Loader" });
    return Loader;
}
